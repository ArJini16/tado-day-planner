# tado-day-planner
Dockerized FastAPI backend that applies time-based temperature schedules to tado° zones per "day type" (workday/homeoffice/away/free), with YAML-driven plans and AES-GCM authenticated endpoints.
